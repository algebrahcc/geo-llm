registry=https://registry.npmmirror.com/
# 指向已存在的全局 pnpm store，避免 store-dir 相对路径解析异常
store-dir = "D:\.pnpm-store\v11"


# 强制提升所有依赖到扁平的 node_modules
# 模拟 npm/yarn 的结构，避免路径解析问题
shamefully-hoist = true

# 严格模式下不检查 peer 依赖版本（避免很多不必要的警告）
strict-peer-dependencies = false

# 自动安装 peer 依赖
auto-install-peers = true

# 解决 windows 路径过长问题（如果文件路径超过 260 字符）
node-linker = hoisted

# 将 pnpm-lock.yaml 复制到各 workspace 子包目录下
copy-workspace-lockfile = true

# 将依赖从 pnpm store 实际复制（而非硬链接/符号链接）到 node_modules/.pnpm
# 适用于需要打包迁移或符号链接不可用的环境
copy-pnpmstore = true

# 离线优先：优先使用本地 store 缓存，避免访问网络 registry
prefer-offline = true
